# SeismiStats Frontend Environment Variables

# Mapbox token for map visualization
VITE_MAPBOX_TOKEN=your_mapbox_token_here

# V2 API Configuration
# Set to 'true' to use the V2 backend API instead of direct USGS calls
VITE_USE_API=false

# API URL (defaults to http://localhost:3000 if not set)
# VITE_API_URL=http://localhost:3000
