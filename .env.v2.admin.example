# SeismiStats V2 Admin Stack Environment Variables
# =================================================
# For the ADMIN (write-enabled) deployment of SeismiStats V2
# ⚠️  DO NOT expose this stack to the public internet!
#
# Deployment: docker-compose.v2.admin.yml
# Prerequisites: Public stack (docker-compose.v2.yml) must be deployed first
#
# Copy this file to .env.v2.admin.local and fill in your values.
# =================================================

# ===================
# DATABASE (Required)
# ===================
# Must match the public stack's database credentials!
DB_USER=seismistats
DB_PASSWORD=CHANGE_ME_secure_password_here
DB_NAME=seismistats

# ===================
# ADMIN API CONFIGURATION
# ===================
# URL where the ADMIN API is accessible (used by admin frontend)
# This should be an internal URL, not exposed publicly
ADMIN_API_URL=http://seismistats-admin_api-admin:3000

# CORS origin for admin frontend
# Typically the internal admin frontend URL
ADMIN_CORS_ORIGIN=http://localhost:5174

# ===================
# SYNC SCHEDULE
# ===================
# Cron expression for automatic USGS sync
# Default: every 6 hours
# More frequent: "0 * * * *" (hourly)
# Less frequent: "0 0 * * *" (daily)
SYNC_CRON=0 */6 * * *

# ===================
# SECURITY NOTES
# ===================
# 1. Admin stack runs with ADMIN_MODE=true (can modify database)
# 2. Access via:
#    - SSH tunnel: ssh -L 5174:localhost:5174 your-server
#    - VPN: http://server-ip:5174
#    - Internal network only
# 3. NEVER expose port 5174 to the public internet
# 4. Consider adding HTTP basic auth via reverse proxy
