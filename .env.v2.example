# SeismiStats V2 Public Stack Environment Variables
# ==================================================
# For the PUBLIC (read-only) deployment of SeismiStats V2
#
# Deployment: docker-compose.v2.yml
# Image: ghcr.io/dlarsen395/seismistats:v2.x.x (frontend)
#        ghcr.io/dlarsen395/seismistats-api:v2.x.x (api)
#
# Copy this file to .env.v2.local and fill in your values.
# ==================================================

# ===================
# DATABASE (Required)
# ===================
# PostgreSQL database credentials
DB_USER=seismistats
DB_PASSWORD=CHANGE_ME_secure_password_here
DB_NAME=seismistats

# ===================
# API CONFIGURATION
# ===================
# URL where the API is accessible (used by frontend)
# In Portainer with NPM proxy, this is typically your public API domain
API_URL=https://api.seismistats.example.com

# CORS origin - the domain(s) that can access the API
# Use * for any origin, or specify your frontend domain
CORS_ORIGIN=https://seismistats.example.com

# ===================
# NOTES
# ===================
# - The public stack runs with ADMIN_MODE=false (hardcoded in compose)
# - Database seeding/sync must be done via the admin stack
# - USGS_SYNC_ENABLED is false on public instances
